# lesson_10
# Документация проекта

## Описание базового синтаксиса записи и форматирования

Проект использует следующие технологии и синтаксис:

1. Python: Основной язык программирования для написания тестов.
2. Selenium: Библиотека для автоматизации взаимодействия с веб-браузером.
3. Pytest: Фреймворк для написания и запуска тестов.
4. Allure: Инструмент для генерации отчетов о выполнении тестов.

### Форматирование кода

- Код форматируется в соответствии с PEP 8 (стиль написания кода на Python).
- Используются docstrings для документирования методов и функций.
- Все шаги теста размечаются с помощью @allure.step или with allure.step для улучшения читаемости отчетов.

---

## Инструкция по запуску тестов для формирования отчета Allure

1. Установите необходимые зависимости:
pip install -r requirements.txt
2. Запустите тесты с генерацией отчета Allure:
pytest --alluredir=./allure-results
Эта команда запустит все тесты и сохранит результаты в директорию ./allure-results.

---

## Инструкция по просмотру сформированного отчета Allure

1. После завершения тестов, чтобы просмотреть отчет, выполните команду:
allure serve ./allure-results
Эта команда запустит локальный сервер и откроет отчет в браузере.

2. В отчете Allure вы увидите:
   - Тест: test_01_calc.py.
   - Название теста: Автотест проверки функциональнсоти калькулятора на сайте.
   - Описание теста: Проверяется правильность работу калькулятора.
   - Шаги теста:
     - Открытие страницы калькулятора.
     - Установление задержки.
     - Нажатие кнопок.
     - Ожидание результата вычисления.
     - Проверка результата вычисления.
   - Результат: Успешное выполнение теста. 
   Ожидаемый результат: Через 45 секунд в окне калькулятора отображается число 15. 
   Фактический результат: В окне калькулятора результат 15 появляется через 45 секунд.

      - Тест: test_calc.py.
   - Название теста: Автотест проверки функциональности интернет-магазина.
   - Описание теста: Проверяется полный процесс оформления заказа.
   - Шаги теста: 
     - Открытие страницы авторизации.
     - Заполнение формы авторизации ('standard_user'), ('secret_sauce').
     - Отправление заполненной формы авторизации.
     - Добавление товара в корзину.
     - Переход в корзину покупок.
     - Заполнение формы доставки('Анастасия'), ('Иванова'), ('987654').
     - Завершение оформление заказа
     - Получение общей стоимости заказа
   - Результат: Успешное выполнение теста. 
   Ожидаемый результат: В конце оформления заказа итоговая сумма заказа равна $58.29. 
   Фактический результат: В конце оформления заказа итоговая сумма составляет $58.29.